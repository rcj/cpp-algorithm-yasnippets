# -*- mode: snippet -*-
# name: count_if(InIt beg, InIt end, UnPred p) -> InIt::difference_type
# key: cni
# --
${1:auto ${2:n} = }`(rcj-cc-mode-std-prefix-for-file)`::count_if(${3:`(rcj-cc-mode-std-prefix-for-file)`::begin(${4:container})}, ${5:`(rcj-cc-mode-std-prefix-for-file)`::end($4)}, ${6:[](auto ${7:const& }elem) { ${8:return elem == 42;} }});$0